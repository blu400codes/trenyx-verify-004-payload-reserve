# Detection matrix — the target's own suite vs. planted defects

Baseline: 0 pre-existing failures (baseline_failures.txt).

**Result: 11 / 13 plantable defects caught by the target's tests.**

| id | defect | file | result | new failing tests |
|---|---|---|---|---|
| D01 | capacity check admits one unit over quantity | `src/services/AvailabilityService.ts` | **CAUGHT** | A3: a neighbor service bufferTimeAfter blocks a back-to-back booking, A4: a multi-item booking only blocks each resource for its own item window, A5: two items in one create on the same resource cannot overlap, PATCH guestCount over per-guest capacity is rejected, PATCH startTime onto an occupied slot is rejected, POST /reserve/hold returns a token and blocks other bookers, a bounded flexible booking is conflict-checked on the expanded pool, a held slot is absent from getAvailableSlots when holdsSlug is passed, a hold request for an already-booked slot is refused, allows booking with guestCount: 5 because 15+5=20 is exactly at capacity, an active hold blocks another customer from booking the slot, blocks the WHOLE resource: quantity > 1 still rejects (units = quantity, not 1), concurrent POSTs to /reserve/book yield exactly one booking, no 500s, conflict-checks the top-level resource when items[] omits it, counts an items[] resource that starts before the top-level startTime, drops a slot when the shared chair pool is fully booked at that time, logs conflict_check and conflict_reject when a booking conflicts, multi-resource booking uses per-item service buffers for conflict detection, prevents double-booking on same resource, rejects a booking overlapping an external interval, rejects a booking when one of the items conflicts with an existing reservation, rejects a fourth overlapping reservation when all units are booked, rejects a second booking when the required pool is full, rejects a standalone booking on a resource already held only in another booking's items[], rejects booking with guestCount: 6 because 15+6=21 exceeds capacity of 20, reports all_slots_taken for a fully booked full-day service, reports all_slots_taken when the day is fully booked, retry recovers the FULL capacity of a quantity-3 resource under a burst of holds, retrying transient write conflicts recovers the full capacity of 3, returns one fewer slot after booking the first available slot, sequential double-booking is rejected (control), still rejects a genuinely conflicting booking against a stored row with an inverted top-level window, unions service.requiredResources so a full pool removes a slot |
| D02 | before-buffer dropped from the blocked window | `src/utilities/slotUtils.ts` | **CAUGHT** | multi-resource booking uses per-item service buffers for conflict detection |
| D03 | conflict detected but not enforced (rejection skipped) | `src/hooks/reservations/validateConflicts.ts` | **CAUGHT** | A3: a neighbor service bufferTimeAfter blocks a back-to-back booking, A4: a multi-item booking only blocks each resource for its own item window, A5: two items in one create on the same resource cannot overlap, PATCH guestCount over per-guest capacity is rejected, PATCH startTime onto an occupied slot is rejected, POST /reserve/hold returns a token and blocks other bookers, a bounded flexible booking is conflict-checked on the expanded pool, an active hold blocks another customer from booking the slot, blocks the WHOLE resource: quantity > 1 still rejects (units = quantity, not 1), concurrent POSTs to /reserve/book yield exactly one booking, no 500s, conflict-checks the top-level resource when items[] omits it, counts an items[] resource that starts before the top-level startTime, logs conflict_check and conflict_reject when a booking conflicts, multi-resource booking uses per-item service buffers for conflict detection, prevents double-booking on same resource, rejects a booking overlapping an external interval, rejects a booking when one of the items conflicts with an existing reservation, rejects a fourth overlapping reservation when all units are booked, rejects a second booking when the required pool is full, rejects a standalone booking on a resource already held only in another booking's items[], rejects any additional booking once capacity is fully consumed (20/20), rejects booking with guestCount: 6 because 15+6=21 exceeds capacity of 20, retrying transient write conflicts recovers the full capacity of 3, sequential double-booking is rejected (control), still rejects a genuinely conflicting booking against a stored row with an inverted top-level window |
| D04 | bookingLock taken for only the first claimed resource | `src/hooks/reservations/acquireBookingLock.ts` | **ESCAPED** |  |
| D05 | first idempotency-key replay accepted | `src/hooks/reservations/checkIdempotency.ts` | **ESCAPED** |  |
| D06 | exception range end date exclusive (last leave day bookable) | `src/utilities/scheduleUtils.ts` | **CAUGHT** | A11: an exception on one of a resource’s schedules blocks the whole resource that day, UTC-midnight instant keys to the prior day in negative-offset zones, UTC-midnight instant stays on the same day in non-negative-offset zones, an exception blocks exactly the Paris calendar day, matches a single-day exception (back-compat), matches inside a range and both boundaries inclusively |
| D07 | consumed hold not released after conversion | `src/endpoints/createBooking.ts` | **CAUGHT** | a booking presenting the holdToken succeeds |
| D08 | only the first item's service/resource checked for active | `src/hooks/reservations/validateActive.ts` | **CAUGHT** | rejects when a service auto-expands an inactive required pool, uses an indexed path for a multi-resource booking |
| D09 | cancellationToken echoed in the book response | `src/endpoints/createBooking.ts` | **CAUGHT** | book endpoint does not return the cancellationToken |
| D10 | customer id honoured from the caller on the collection route | `src/hooks/reservations/enforceCustomerOwnership.ts` | **CAUGHT** | forces a non-privileged customer to book for themselves |
| D11 | cancellation notice period never enforced | `src/hooks/reservations/validateCancellation.ts` | **CAUGHT** | beforeBookingCancel does NOT fire when the notice period rejects the cancel (A8), nests the hook message where extractErrorMessage looks for it, rejects cancellation within notice period |
| D12 | illegal status transition accepted | `src/services/AvailabilityService.ts` | **CAUGHT** | rejects completed -> pending transition, validateTransition: rejects invalid completed -> pending |
| D13 | per-guest occupancy counted as one unit | `src/services/AvailabilityService.ts` | **CAUGHT** | PATCH guestCount over per-guest capacity is rejected, rejects any additional booking once capacity is fully consumed (20/20), rejects booking with guestCount: 6 because 15+6=21 exceeds capacity of 20 |
| N01 | date-only exception/manual-slot dates shift a day west of UTC |  | **NATIVE** |  |
| N02 | plain-install customers read/mutate all reservations + customer PII |  | **NATIVE** |  |
| N03 | delete skips cancellation policy + cancel hooks |  | **NATIVE** |  |
| N04 | guestCount 1.5 accepted on the collection (hold endpoint rejects it) |  | **NATIVE** |  |
